// Curly brackets and 'return' can be ignored if only one expression present
var multiply = (x, y) => x * y;



// Parentheses are optional when only one parameter is present
var addOne = x => x + 1;


// Parentheses are required when no parameters are present.
var printHello = () => { console.log('Hello'); };


